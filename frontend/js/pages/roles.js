/**
 * Page-specific script for roles.html. The page is entirely read-only
 * static content (no CRUD, no filtering), so this only turns on the
 * shared shell chrome.
 */

import { initShell } from "../core/shell.js";

initShell();
