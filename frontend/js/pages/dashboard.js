/**
 * Page-specific script for dashboard.html. The dashboard has no unique
 * interactive behavior of its own in this milestone — every summary card,
 * activity item, and table row is static placeholder content — so this
 * file's only job is turning on the shared shell chrome.
 */

import { initShell } from "../core/shell.js";

initShell();
